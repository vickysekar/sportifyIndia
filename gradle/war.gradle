apply plugin: "war"

bootWar {
    mainClass = "com.sportifyindia.app.SportifyingIndiaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
